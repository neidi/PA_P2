#include "queue_fixed.h"

#define MAX_SIZE 3

void init(Queue *q) {
    q->front = 0;
    q->rear = -1;
    q->size = 0;
}

bool isEmpty(Queue *q) {
}

bool isFull(Queue *q) {
}

bool enqueue(Queue *q, int value) {
}

int dequeue(Queue *q) {
}

int peek(Queue *q) {
}
